[project]
name = "temperature-flink-job"
version = "0.1.0"
description = "PyFlink job for filtering sub-zero temperatures"
requires-python = ">=3.11"
dependencies = [
  "apache-flink==1.20.0",
]

[tool.uv]
package = false
