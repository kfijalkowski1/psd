[project]
name = "temperature-consumer"
version = "0.1.0"
description = "Simple Kafka output-topic consumer demo"
requires-python = ">=3.11"
dependencies = [
  "kafka-python>=2.0.2",
]

[tool.uv]
package = false
